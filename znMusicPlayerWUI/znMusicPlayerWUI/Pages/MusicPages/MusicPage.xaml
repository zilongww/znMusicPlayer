<Page
    x:Class="znMusicPlayerWUI.Pages.MusicPages.MusicPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:znMusicPlayerWUI"
    xmlns:data="using:znMusicPlayerWUI.DataEditor"
    xmlns:controls="using:znMusicPlayerWUI.Controls"
    xmlns:m="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:brush="using:CommunityToolkit.WinUI.UI.Media"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="pageRoot" Loaded="pageRoot_Loaded"
    mc:Ignorable="d" Opacity="1" IsHitTestVisible="True">

    <Page.Resources>
        <StaticResource x:Key="TextForeground" ResourceKey="ButtonForegroundThemeBrush"/>
    </Page.Resources>

    <Grid>
        <controls:Imagezn x:Name="BackgroundBaseImage" IsHitTestVisible="False"
                          VerticalAlignment="Center" HorizontalAlignment="Center"
                          Stretch="UniformToFill" Loaded="BackgroundBaseImage_Loaded"/>
        <TextBlock x:Name="BridgeTb" TextAlignment="Center" HorizontalAlignment="Left"/>
        <Rectangle>
            <Rectangle.Fill>
                <!--<SolidColorBrush Color="#55000000"/>-->
                <AcrylicBrush x:Name="BlueImageAcrylicBrush" TintLuminosityOpacity="0.85" TintOpacity="0.0" FallbackColor="{ThemeResource MusicPageImageBlurFallbackColor}" TintColor="{ThemeResource BlurImageAcrylicBrush}" Opacity="1"/>
            </Rectangle.Fill>
        </Rectangle>
        <Rectangle Fill="{ThemeResource ApplicationPageBackgroundThemeBrush}" Opacity="0.0001"/>
        <Grid Padding="0,48,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.8*" x:Name="InfoPageColumn"/>
                <ColumnDefinition Width="1.35*" x:Name="LrcPageColumn"/>
            </Grid.ColumnDefinitions>
            <Grid x:Name="InfoBaseGrid" VerticalAlignment="Stretch" Padding="40,0,40,40" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="0.06*" MinHeight="34"/>
                    <RowDefinition Height="0.17*"/>
                </Grid.RowDefinitions>
                <StackPanel>
                    <StackPanel.Transitions>
                        <TransitionCollection>
                            <RepositionThemeTransition/>
                        </TransitionCollection>
                    </StackPanel.Transitions>
                    <TextBlock x:Name="InfoBaseTitle" TextAlignment="Center" FontSize="24" IsTextSelectionEnabled="True"/>
                    <TextBlock x:Name="InfoBaseArtist" TextAlignment="{Binding ElementName=InfoBaseTitle,Path=TextAlignment}" IsTextSelectionEnabled="True"/>
                </StackPanel>
                <Grid Grid.Row="1" Margin="0,32,0,32"
                      HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Grid.Transitions>
                        <TransitionCollection>
                            <RepositionThemeTransition/>
                        </TransitionCollection>
                    </Grid.Transitions>
                    <Border CornerRadius="8" Margin="12">
                        <controls:Imagezn x:Name="AlbumImageBase" IsMassImage="False"/>
                    </Border>
                </Grid>
                <Grid Grid.Row="2">
                    <Grid.Transitions>
                        <TransitionCollection>
                            <RepositionThemeTransition/>
                        </TransitionCollection>
                    </Grid.Transitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="NowPlayTimeTb"
                               Text="00:00"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Grid.Column="0">
                        <TextBlock.Transitions>
                            <TransitionCollection>
                                <RepositionThemeTransition/>
                            </TransitionCollection>
                        </TextBlock.Transitions>
                    </TextBlock>
                    <Slider x:Name="PlaySlider"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Center"
                            IsThumbToolTipEnabled="False"
                            Padding="12,0,12,0"
                            Grid.Column="1"
                            ValueChanged="PlaySlider_ValueChanged">
                        <Slider.Background>
                            <brush:BackdropGammaTransferBrush RedDisable="True" GreenDisable="True" BlueDisable="True"
                                                              AlphaAmplitude="1" AlphaExponent="1" AlphaOffset="1"/>
                        </Slider.Background>
                    </Slider>
                    <TextBlock x:Name="NowAtherTimeTb"
                               Text="00:00"
                               FontSize="12"
                               VerticalAlignment="Center"
                               Grid.Column="2">
                        <TextBlock.Transitions>
                            <TransitionCollection>
                                <RepositionThemeTransition/>
                            </TransitionCollection>
                        </TextBlock.Transitions>
                    </TextBlock>
                </Grid>
                <Grid Grid.Row="3">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <StackPanel.Transitions>
                            <TransitionCollection>
                                <RepositionThemeTransition/>
                            </TransitionCollection>
                        </StackPanel.Transitions>
                        <ToggleButton x:Name="LrcButton" Padding="12" Width="44" Height="44"
                                      Checked="LrcButton_Checked" Unchecked="LrcButton_Unchecked"
                                      Background="Transparent" BorderThickness="0" FontWeight="Normal">
                            <FontIcon Glyph="词" FontSize="14" FontFamily="{StaticResource ContentControlThemeFontFamily}"/>
                        </ToggleButton>
                    </StackPanel>
                    <Grid Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.Transitions>
                            <TransitionCollection>
                                <RepositionThemeTransition/>
                            </TransitionCollection>
                        </Grid.Transitions>
                        <StackPanel Orientation="Horizontal">
                            <Button x:Name="BeforeButton" Padding="12" Width="44" Height="44" Margin="0,0,4,0"
                                    IsEnabled="{Binding ElementName=PlayButton, Path=IsEnabled}"
                                    Background="Transparent" BorderThickness="0" FontWeight="Normal" Click="BeforeButton_Click">
                                <Path Data="{StaticResource 上一首}"
                                      Fill="{ThemeResource ButtonForegroundThemeBrush}"
                                      Stretch="UniformToFill" Margin="2"/>
                            </Button>
                            <Button x:Name="PlayButton" Padding="13" Width="50" Height="50" CornerRadius="25"
                                    Background="Transparent" BorderThickness="0" FontWeight="Normal" Click="PlayButton_Click">
                                <controls:MediaPlayStateViewer x:Name="MediaPlayStateViewer1" PlaybackState="Paused" Margin="1"/>
                            </Button>
                            <Button x:Name="NextButton" Padding="12" Width="44" Height="44" Margin="4,0,0,0"
                                    IsEnabled="{Binding ElementName=PlayButton, Path=IsEnabled}"
                                    Background="Transparent" BorderThickness="0" FontWeight="Normal" Click="NextButton_Click">
                                <Path Data="{StaticResource 下一首}"
                                      Fill="{ThemeResource ButtonForegroundThemeBrush}"
                                      Stretch="UniformToFill" Margin="2"/>
                            </Button>
                        </StackPanel>
                        <ProgressRing x:Name="AudioLoadingProressRing"
                                      Width="54" IsIndeterminate="True"
                                      Height="{Binding ElementName=AudioLoadingProressRing, Path=ActualWidth, Mode=TwoWay}"/>
                    </Grid>
                    <CommandBar Grid.Column="3"
                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                IsDynamicOverflowEnabled="True"
                                OverflowButtonVisibility="Auto"
                                DefaultLabelPosition="Collapsed"
                                Background="Transparent">
                        <CommandBar.Transitions>
                            <TransitionCollection>
                                <RepositionThemeTransition/>
                            </TransitionCollection>
                        </CommandBar.Transitions>
                        <AppBarButton x:Name="VloumeButton" Label="音量" Style="{ThemeResource AppBarStyle}"
                                      Background="Transparent" BorderThickness="0" FontWeight="Normal" Click="VloumeButton_Click">
                            <AppBarButton.Icon>
                                <PathIcon Data="{StaticResource 音量2}"/>
                            </AppBarButton.Icon>
                        </AppBarButton>
                        <AppBarButton x:Name="PlayListButton" Label="播放列表" Style="{ThemeResource AppBarStyle}"
                                      Background="Transparent" BorderThickness="0" FontWeight="Normal" Click="PlayListButton_Click">
                            <AppBarButton.Icon>
                                <PathIcon Data="{StaticResource 播放列表}"/>
                            </AppBarButton.Icon>
                        </AppBarButton>
                    </CommandBar>
                </Grid>
            </Grid>

            <Grid x:Name="LrcBaseGrid" Grid.Column="1" PointerWheelChanged="LrcBaseListView_PointerWheelChanged">
                <Grid.Transitions>
                    <TransitionCollection>
                        <RepositionThemeTransition/>
                    </TransitionCollection>
                </Grid.Transitions>
                <ListView x:Name="LrcBaseListView"
                          Loaded="LrcBaseListView_Loaded"
                          Background="Transparent"
                          SelectionChanged="LrcBaseListView_SelectionChanged"
                          ScrollViewer.VerticalScrollBarVisibility="Hidden"
                          IsFocusEngagementEnabled="True"
                          SizeChanged="LrcBaseListView_SizeChanged"
                          HorizontalContentAlignment="Stretch">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="data:LyricData">
                            <TextBlock TextAlignment="{Binding ElementName=BridgeTb, Path=TextAlignment}"
                                           TextWrapping="Wrap">
                                    <Run Text="{x:Bind Lyric}" FontSize="29"/><LineBreak/><Run Text="{x:Bind Translate}" FontSize="22"/>
                            </TextBlock>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <ListViewItemPresenter
                                            BorderBrush="Transparent"
                                            Background="{x:Null}"
                                            Foreground="{ThemeResource MusicPageLrcForeground}"
                                            SelectedForeground="{ThemeResource AccentAAFillColorTertiaryBrush}"
                                            ContentTransitions="{TemplateBinding ContentTransitions}"
                                            SelectionCheckMarkVisualEnabled="True"
                                            CheckBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}"
                                            CheckBoxBrush="{ThemeResource SystemControlForegroundBaseMediumHighBrush}"
                                            DragBackground="{ThemeResource ListViewItemDragBackgroundThemeBrush}"
                                            DragForeground="{ThemeResource ListViewItemDragForegroundThemeBrush}"
                                            FocusBorderBrush="{ThemeResource SystemControlForegroundAltHighBrush}"
                                            FocusSecondaryBorderBrush="{ThemeResource SystemControlForegroundBaseHighBrush}"
                                            PlaceholderBackground="Transparent"
                                            PointerOverBackground="Transparent"
                                            PointerOverBorderBrush="Transparent"
                                            SelectedBackground="Transparent"
                                            SelectedPointerOverBackground="Transparent"
                                            SelectedPressedBackground="Transparent"
                                            PressedBackground="Transparent"
                                            DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}"
                                            DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}"
                                            ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                                            ContentMargin="{TemplateBinding Padding}"
                                            CheckMode="Overlay"
                                            MinWidth="0"
                                            Margin="0,16">
                                        </ListViewItemPresenter>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Grid>
        <Button x:Name="CloseMusicPageButton" Click="Button_Click"
                Background="Transparent" BorderBrush="Transparent"
                VerticalAlignment="Top" HorizontalAlignment="Left"
                HorizontalContentAlignment="Left" CornerRadius="0,0,4,0"
                Width="84" Height="48" Padding="12" BorderThickness="0,1,1,1"
                ToolTipService.ToolTip="一目了然，不言而喻。">
            <AnimatedIcon>
                <AnimatedIcon.Source>
                    <m:AnimatedChevronDownSmallVisualSource/>
                </AnimatedIcon.Source>
            </AnimatedIcon>
        </Button>
    </Grid>
</Page>
